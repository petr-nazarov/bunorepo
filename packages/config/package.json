{
  "name": "config",
  "module": "index.ts",
  "version": "0.3.12",
  "type": "module",
  "scripts": {
    "get:version": "../../bin/get-package-version.sh",
    "build": "echo done",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "fix": "biome check . --apply",
    "check": "biome ci ."
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
